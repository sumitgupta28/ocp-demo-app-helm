apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocp-demo-app-db.fullname" . }}-test-connection"
  labels:
    {{- include "ocp-demo-app-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocp-demo-app-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
