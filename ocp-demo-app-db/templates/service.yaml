apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocp-demo-app-db.fullname" . }}
  labels:
    {{- include "ocp-demo-app-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocp-demo-app-db.selectorLabels" . | nindent 4 }}
